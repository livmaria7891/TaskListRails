<h1>Add a task</h1>

<!-- <%= form_for @this_task, as: :task, url: create_path do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_area :description %>

<!- Why does the button say Create Task? How do I change it? -->
<%#= f.submit %>

<%# end %> -->

<%= render partial: "form", locals: {action_name: 'create'} %>
